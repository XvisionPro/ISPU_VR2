using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class WeaponSlot : MonoBehaviour
{
    /*[SerializeField] private Image background;
    [SerializeField] private Text ammoText;
    private Weapon weapon;
    public void SetWeapon(Weapon weapon)
    {
        this.weapon = weapon;

        // Update the slot's background and ammo text to match the weapon
        background.sprite = weapon.GetComponent<SpriteRenderer>().sprite;
        ammoText.text = weapon.GetCurrentAmmo() + " / " + weapon.GetMaxAmmo();
    }

    public void Select(bool isSelected)
    {
        // Highlight the slot if it's selected
        background.color = isSelected ? Color.gray : Color.white;
    }*/
}
